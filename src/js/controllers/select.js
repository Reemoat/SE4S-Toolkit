// The object controls a click when the palette has "Select" highlighted
var SELECT = {
    /**
     * Handle canvas clicks when the palette has "Select highlighted"
     */
    canvasClick: function () {
    }
}
